import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
   logo:{
    position: 'absolute',
    top: '15px',
    left: '30px'
   },
   text: {
      marginBottom:"25px"
   }
})

export {useStyles};