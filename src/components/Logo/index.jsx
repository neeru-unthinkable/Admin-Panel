import React from "react";

const Logo = ({ className }) => {
  return (
    <img
      className={className}
      src="assets/PinelabsLogo.png"
      alt="logo"
    />
  );
};

export default Logo;
